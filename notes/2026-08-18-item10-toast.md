# Item 10: TOAST (The Oversized-Attribute Storage Technique)

**Date:** 2026-08-18  
**Topic:** PostgreSQL TOAST Architecture, 2KB Inline Threshold (`TOAST_TUPLE_THRESHOLD`), 18-Byte `ToastPointer`, 2KB `ToastChunk` Slicing, and Multi-Page Auxiliary Storage  
**Status:** Complete & Verified  

---

## 1. Directory Structure & Attached Code Files

- **TOAST Header**: [`include/pg/toast.h`](../include/pg/toast.h)
- **TOAST Implementation**: [`src/toast.cpp`](../src/toast.cpp)
- **Unit Test Suite**: [`tests/test_toast.cpp`](../tests/test_toast.cpp)

---

## 2. The Problem TOAST Solves

In PostgreSQL:
1. Disk pages are fixed at **8KB** (`PAGE_SIZE = 8192`), and rows **cannot span across physical page boundaries**.
2. If a user inserts a **20KB text document**, a **100KB JSON payload**, or a **5MB image**, it physically cannot fit on a single 8KB slotted page.
3. Even if a 4KB row *could* fit, putting large attributes inline on the main heap table would cause catastrophic **table bloat**, drastically reducing the number of rows per page and crippling sequential scan performance.

**TOAST (The Oversized-Attribute Storage Technique)** solves this by storing large variable-length attributes in an auxiliary side table and keeping only an 18-byte pointer in the main tuple:

```text
       MAIN HEAP TABLE (Page 0)                 AUXILIARY TOAST TABLE (Side File)
 +----------------------------------+          +----------------------------------+
 | Slot 1: item_id=1, price=$100    |          | Chunk (id=1, seq=0) [2048 bytes] |
 |   ToastPointer (18 Bytes):       |          | Chunk (id=1, seq=1) [2048 bytes] |
 |   [id=1, raw_size=20481, chunks=11]───►     | Chunk (id=1, seq=2) [2048 bytes] |
 |                                  |          | ...                              |
 | Slot 2: item_id=2, price=$200    |          | Chunk (id=1, seq=10)[  81 bytes] |
 |   ToastPointer (18 Bytes):       |          +----------------------------------+
 |   [id=2, raw_size=20481, chunks=11]───►     | Chunk (id=2, seq=0) [2048 bytes] |
 +----------------------------------+          +----------------------------------+
```

---

## 3. Thresholding & Slicing Strategy

PostgreSQL applies a **$\frac{1}{4}$ Page Size Rule**:
- **Inline Payload ($\le 2048$ Bytes)**: Stored directly inline in the tuple payload (`ToastStorageType::INLINE`). Zero auxiliary table overhead.
- **Out-of-Line Payload ($> 2048$ Bytes)**: Sliced into individual **2KB chunks** (`TOAST_CHUNK_SIZE = 2048`) and written to the dedicated TOAST relation (`ToastStorageType::OUT_OF_LINE`).

---

## 4. The 18-Byte `ToastPointer`

When an attribute is TOASTed, the main table replaces the multi-kilobyte data with a packed 18-byte pointer:

```cpp
#pragma pack(push, 1)
struct ToastPointer {
    uint64_t toast_id{0};       // Unique 64-bit ID identifying this toasted value (8B)
    uint32_t raw_size{0};       // Full uncompressed byte size (4B)
    uint32_t chunk_count{0};    // Number of 2KB chunks stored in TOAST table (4B)
    uint16_t flags{0};          // Storage & compression status flags (2B)
};
#pragma pack(pop)
```

---

## 5. Multi-Page Reassembly

Upon executing a query that reads a TOASTed column:
1. The engine inspects `ToastPointer`.
2. Fetches all $N$ chunks $0, 1, \dots, \text{chunk\_count}-1$ from the auxiliary TOAST table in sequential order.
3. Glues the chunks back together in RAM into the original uncompressed string or binary payload.

---

## 6. Verification Results (`tests/test_toast.cpp`)

```text
--- REPRODUCING POSTGRESQL TOAST (OVERSIZED ATTRIBUTE STORAGE) ---
[Step 1] Storing small 500-byte string (Inline threshold)...
 -> Stored inline inside tuple! TOAST table chunks: 0.
 -> Verified: Small inline text reassembled byte-for-byte.

[Step 2] Storing oversized 20KB text document (> 2KB threshold)...
 -> TOAST Splicing successful! 20KB payload sliced into 11 chunks of 2KB.
 -> Main tuple stores only an 18-byte ToastPointer (toast_id=1, raw_size=20481).
[Step 3] Fetching and reconstructing 20KB string from TOAST chunks...
 -> Verified: 20KB string reconstructed byte-for-byte from 11 chunks!

[Step 4] Storing massive 100KB JSON payload across multiple TOAST pages...
 -> Verified: 100KB payload spanned 20 8KB TOAST pages and reassembled perfectly!

[Step 5] Verifying Main Slotted Page Bloat Prevention...
 -> Inserted 10 rows with 200KB total data into main table.
 -> Main Heap Table Page Count: 1 (All 10 rows fit on Page 0 with zero page bloat!)
 -> All 10 large product descriptions reconstructed byte-for-byte!

>>> ITEM 10 (TOAST / OVERSIZED ATTRIBUTE STORAGE) TESTS PASSED SUCCESSFULLY! <<<
```

---

## 8. Quiz Diagnostics & Graded Mechanics

### Q1 · 2KB TOAST Threshold Rationale (`TOAST-THRESHOLD`)
- **Question**: Why does PostgreSQL set the default TOAST threshold at ~2KB ($\approx \frac{1}{4}$ page size) rather than waiting until 8KB?
- **Answered**: Option 2 (PostgreSQL aims to ensure at least 4 tuples fit per 8KB page to maintain high tuple density and prevent severe page fragmentation/bloat during sequential scans) ✅
- **Mechanism**:
  If 6KB or 7KB tuples were allowed directly inline on 8KB slotted pages, each page could hold only 1 row, leaving huge amounts of unusable fragmented free space. Slicing at 2KB guarantees that every 8KB table page maintains high packing density.

---

### Q2 · ToastPointer Indirection Layout (`TOAST-POINTER-INDIRECTION`)
- **Question**: When a 50KB text column is inserted into a row, what is physically written to the main table's page?
- **Answered**: Option 3 (The 50KB text is sliced into 25 chunks and written to the auxiliary TOAST table; the main slotted page stores only a tiny **18-byte `ToastPointer`** containing `toast_id`, `raw_size=50000`, `chunk_count=25`) ✅
- **Mechanism**:
  The main heap tuple is completely decoupled from the large binary payload. The 18-byte `ToastPointer` acts as a lightweight foreign reference, keeping main heap tuples small, predictable, and cache-friendly.

---

### Q3 · Analytical Scan & Cache Efficiency (`TOAST-SCAN-EFFICIENCY`)
- **Question**: How does TOAST improve the performance of queries that do not access the large TOASTed column?
- **Answered**: Option 2 (The main table remains compact, allowing sequential scans to read only a few megabytes of main table pages into `shared_buffers`, completely bypassing hundreds of megabytes of cold TOAST chunk pages) ✅
- **Mechanism**:
  Because large payloads reside in a separate auxiliary relation file, queries scanning metadata or un-toasted columns (like `SELECT count(*)`, `SELECT price`) never load or touch TOAST pages from disk. This saves massive disk I/O bandwidth and preserves RAM buffer pool cache locality.

